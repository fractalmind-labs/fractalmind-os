# Glossary

- **Spec**：标准层；定义 contract / schema / compatibility
- **ROM**：具体发行版；定义 templates / assets / defaults / release notes
- **Contract**：对 workspace / ROM 的最低可验证要求
- **Manifest**：ROM 对自身内容与兼容性的结构化声明
- **Compatibility**：spec version 与 ROM version 的兼容关系
