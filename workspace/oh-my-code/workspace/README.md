# workspace/

This directory is for local clones of repositories you want this `oh-my-code` workflow to operate on.

- Repos under `workspace/` are discovered automatically (see `workflows/github_issues.md`).
- This directory is gitignored; do not add submodules here.

