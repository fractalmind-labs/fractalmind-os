interface:
  display_name: "Use Codex App"
  short_description: "List and message local Codex/ChatGPT App agents"
  default_prompt: "Use $use-codex-app to list sidebar-named Codex/ChatGPT App agents, resolve a named session, or deliver a message through the current app CDP renderer bridge, guarded visible-composer fallback, or an explicit app-server WebSocket."
policy:
  allow_implicit_invocation: false
